# Examples

- Build order usually starts with Models, then Theme/AppTheme.swift, then Features files, then App files.
- rule_keys should list only features and UI rules that planned files actually need.
